meta {
  name: List tasks
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/tasks?page=1&limit=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{process.env.ACCESS_TOKEN}}
}

assert {
  res.status: eq 200
  res.body.data: isArray
  res.body.meta.page: eq 1
}
