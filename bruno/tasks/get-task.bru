meta {
  name: Get task
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/tasks/{{taskId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{process.env.ACCESS_TOKEN}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{taskId}}
}
